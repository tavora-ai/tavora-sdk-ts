{
  "name": "tavora-ts-chat-example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Multi-turn agentic chat REPL using the standalone TypeScript SDK. Mirrors examples/chat (Go).",
  "scripts": {
    "build": "tsc",
    "start": "node --enable-source-maps dist/main.js",
    "dev": "tsc --watch"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@tavora/sdk": "file:../.."
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
